# Benchmark outputs

Benchmark reports created by **Gemma 4 on 16 GB — Local AI Server Kit** belong in `benchmarks/results/`.

Suggested first comparison:

- `gemma4:e4b` as the stable default
- `gemma4:26b` as the experimental benchmark target

Optimization topics such as KV cache, Flash Attention, and TurboQuant should be recorded conservatively in the results,
with the exact Ollama version and environment variables used for the run.
